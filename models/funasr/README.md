# FunASR Model Scaffold

This directory is reserved for local FunASR ONNX assets.

Expected subdirectories:

- `paraformer-zh-onnx/`
- `fsmn-vad-onnx/`
- `ct-punc-onnx/`
- `itn-zh/`

The scaffold keeps only manifests and placeholders under version control. Downloaded model binaries stay ignored.
